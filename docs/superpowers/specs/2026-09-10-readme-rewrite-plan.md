# HeritageMind README 重写实施计划

1. 删除旧 README 中重复版本线、过时 Streamlit 主路径、旧端口、冗长端点表和已失真的工程数量描述。
2. 以“定位 → 能力 → 架构 → 快速开始 → 验证 → 工程导航 → 路线图”重建中文 README。
3. 仅采用可复现事实：23 类技艺、65 节点/39 关系、94 条有效检索评测、154 passed/1 skipped、API 8001 与 Vue 5173。
4. 将 Swagger 作为 API 细节入口，不复制维护成本高的完整端点目录。
5. 用文本检查验证不存在旧端口、过时 Streamlit 推荐启动和未完成能力的完成式表述；保留 Git 工作区冲突状态，不提交。
